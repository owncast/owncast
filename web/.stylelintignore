# Generated by style-definitions (style-dictionary) via `npm run build-styles`.
# These are build outputs, not source — lint the token YAML and the
# hand-authored templates under style-definitions/ instead. style-dictionary's
# CSS formatter doesn't follow stylelint-config-standard-scss (e.g.
# custom-property-empty-line-before), so linting the outputs is noise.
public/styles/plugin.css
styles/variables.css
