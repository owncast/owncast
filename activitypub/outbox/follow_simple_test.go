package outbox

import (
	"testing"
)

// Simple compilation test to ensure the functions exist and compile
func TestFollowFunctionsExist(t *testing.T) {
	// This test simply verifies that our follow functions exist and compile
	// More complex integration testing would require full database, crypto, and network setup

	// These functions exist if the package compiles successfully
	t.Log("SendFollowRequest function exists and compiles successfully")
}
