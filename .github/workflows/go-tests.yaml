# This workflow runs Go unit tests across multiple OS and Go versions
# Triggers:
#   - Push/PR with Go file changes (*.go, go.mod, go.sum)
#   - Skips when only web/ directory changes
# Test Matrix:
#   - PRs: Ubuntu + Windows (catch cross-platform regressions before merge)
#   - Develop/Main: Full matrix (ensure cross-platform compatibility)

name: Go Tests

on:
  push:
    branches:
      - develop
    paths-ignore:
      - 'web/**'
  pull_request:
    paths-ignore:
      - 'web/**'

jobs:
  # Detect relevant changes in a standalone job so the test jobs can be gated
  # at the job level: a run with no relevant changes reports as "skipped"
  # instead of a misleading "success".
  changes:
    runs-on: ubuntu-latest
    outputs:
      src_any_changed: ${{ steps.changed-files-yaml.outputs.src_any_changed }}
    steps:
      - uses: actions/checkout@v7

      - name: Get changed files
        id: changed-files-yaml
        uses: tj-actions/changed-files@v47
        with:
          files_yaml: |
            src:
              - '**/*.{go,mod,sum}'

  test:
    needs: changes
    if: needs.changes.outputs.src_any_changed == 'true'
    strategy:
      matrix:
        os: ${{ github.event_name == 'pull_request' && fromJSON('["ubuntu-latest", "windows-latest"]') || fromJSON('["ubuntu-latest", "macos-latest", "windows-latest", "ubuntu-24.04-arm"]') }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: go-test-${{ github.sha }}
          restore-keys: |
            go-test-

      - name: Install go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.5'
          cache: true

      - name: Run tests
        run: go test ./...

  test-bsds:
    # Only run BSD tests on push events (not PRs) for efficiency
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.src_any_changed == 'true'
    runs-on: macos-latest
    strategy:
      matrix:
        os:
          - name: freebsd
            version: 12.2
          - name: openbsd
            version: 6.8

    steps:
      - uses: actions/checkout@v7

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: go-test-${{ github.sha }}
          restore-keys: |
            go-test-

      - name: Install go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.5'
          cache: true

      - name: Run tests
        run: go test ./...
